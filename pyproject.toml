[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "osteo"
version = "0.1.0"
description = "Early Detection of Osteoporosis using Dental X-rays"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]
